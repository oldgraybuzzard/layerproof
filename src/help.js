const shortcutLink=document.createElement('a');shortcutLink.href='#shortcuts';shortcutLink.dataset.topic='shortcuts';shortcutLink.textContent='Use keyboard shortcuts';document.querySelector('[data-topic="saving"]').before(shortcutLink);
document.getElementById('start').insertAdjacentHTML('beforeend','<h3>Start with PDFs only</h3><p>If no spreadsheet exists, choose the PDF folder. LayerProof discovers its PDFs and creates a standard Excel review workbook for the results. Keep that generated workbook with the project and use it to resume later.</p>');
document.getElementById('pages').insertAdjacentHTML('beforeend','<h3>Apply one concern to several pages</h3><p>Choose <strong>Apply concern to pages...</strong>, enter page numbers or ranges such as <strong>2-6, 9</strong>, choose the concern, and apply it. This adds a separate <strong>Page N:</strong> entry for every selected page.</p><p>A bulk concern is an annotation, not proof that a page was reviewed. It does not check any selected page. Open each page and wait for it to render before marking it checked. <strong>Concern recorded; visual review required</strong> identifies an annotated page that still needs review. Use <strong>Next unchecked</strong> to visit the remaining pages; ordinary page navigation does not check an unseen page.</p>');
const shortcutSection=document.createElement('section');shortcutSection.id='shortcuts';shortcutSection.innerHTML='<h2>Keyboard shortcuts</h2><p>Shortcuts work while focus is outside text fields, selects, and dialogs. Use the Shortcuts button in the app header for a quick reminder.</p><table><tr><th>Action</th><th>Shortcut</th></tr><tr><td>Previous or next page</td><td>Left Arrow / Right Arrow</td></tr><tr><td>Check the current page and continue</td><td>N</td></tr><tr><td>Jump to the next unchecked page</td><td>U</td></tr><tr><td>Mark the current page clear</td><td>C</td></tr><tr><td>Focus the page-number box</td><td>G</td></tr><tr><td>Next or previous visible queue document</td><td>J / K</td></tr><tr><td>Last or first visible queue document</td><td>Shift+J / Shift+K</td></tr><tr><td>Add quick concern 1 through 9</td><td>Alt+1 through Alt+9 (Option on Mac)</td></tr><tr><td>Add a quick concern, check the page, and advance</td><td>Shift+Alt+1 through Shift+Alt+9 (Shift+Option on Mac)</td></tr><tr><td>Save to workbook</td><td>Ctrl+S (Command+S on Mac)</td></tr><tr><td>Save and open the next document</td><td>Ctrl+Shift+S (Command+Shift+S on Mac)</td></tr></table><p>Queue shortcuts follow the active filter, search, and page-count sort. Leaving a document still uses the normal unsaved-change confirmation.</p>';document.getElementById('saving').before(shortcutSection);
const search=document.getElementById('search'),topics=[...document.querySelectorAll('main section')];
function filter(){const words=search.value.toLowerCase().trim().split(/\s+/).filter(Boolean);let shown=0;for(const topic of topics){const matches=words.every(word=>topic.textContent.toLowerCase().includes(word));topic.hidden=!matches;document.querySelector(`[data-topic="${topic.id}"]`).hidden=!matches;if(matches)shown++;}document.getElementById('results').textContent=words.length?`${shown} matching topic${shown===1?'':'s'}`:'';document.getElementById('empty').hidden=shown>0;}
search.addEventListener('input',filter);document.getElementById('clear').onclick=()=>{search.value='';filter();search.focus();};document.getElementById('print').onclick=()=>window.print();document.addEventListener('keydown',event=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==='f'){event.preventDefault();search.focus();search.select();}});
