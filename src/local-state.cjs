const fs=require('node:fs');const path=require('node:path');const crypto=require('node:crypto');
function readJSON(filename,fallback){try{return JSON.parse(fs.readFileSync(filename,'utf8'));}catch(e){if(e.code==='ENOENT')return fallback;throw Error('Cannot read saved local data: '+filename+'. '+e.message);}}
function writeJSON(filename,value){fs.mkdirSync(path.dirname(filename),{recursive:true});const temp=filename+'.'+crypto.randomUUID()+'.tmp';try{const fd=fs.openSync(temp,'wx',0o600);try{fs.writeFileSync(fd,JSON.stringify(value,null,2));fs.fsyncSync(fd);}finally{fs.closeSync(fd);}fs.renameSync(temp,filename);}finally{if(fs.existsSync(temp))fs.unlinkSync(temp);}}
function projectKey(workbook){return crypto.createHash('sha256').update(path.resolve(workbook)).digest('hex');}
function draftCompatible(draft,record,signature){return !!record&&draft.key===record.key&&draft.id===record.id&&draft.signature===signature&&draft.baseIssues===record.hasIssues&&draft.baseConcerns===record.concerns&&draft.baseReviewed===record.qcReviewed&&draft.baseReviewer===record.qcReviewer&&draft.baseReviewedOn===record.qcReviewedOn&&(draft.baseAssignment??'')===(record.pdfAssignment??'');}
module.exports={readJSON,writeJSON,projectKey,draftCompatible};
